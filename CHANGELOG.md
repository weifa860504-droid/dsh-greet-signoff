# 更新日志

本文件记录 `dsh-greet-signoff` 的每个发布版本。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-09-18

工程批次：补上回归网、把 51KB 表情索引搬出主包、打包与 CI 就绪。

### 新增

- **单元测试**（`npm test` / `node --test test/`）：用最小 DOM 桩把 `client.js` 当模块加载进 Node，
  通过模块底部新增的 `__test` 钩子直接测里面那些纯函数 —— 覆盖归一化与三档匹配、近似档的短句保护、
  动态变量解析（含 `{daypart}` 多时段候选）、多行分段、配置归一化与夹紧、旧文案表清洗、空行拦截、
  占用百分比与 `k/M` 格式化、颜色插值与色带。13 个用例，跑的是**真代码**，不是复制品。
- **表情索引外置 + 懒加载**：`emoji-zh.json`（1364 条中文名/关键词，89KB）从 `client.js` 里搬出来，
  改由宿主 `GET /api/greet-signoff/emoji-index` 提供；**首次展开表情框时才加载**，
  同时把"扫描 3800 个码位"也推迟到那一刻。主包 `client.js` 从 236KB 降到 **152KB**，
  页面加载时不再同步跑字体测量。

### 变更

- `docs/ci-workflow.yml` 增加"跑单测"与"表情索引可解析"两步（启用见 `docs/RELEASING.md`）。
- `package.json` 增加 `test` 脚本；`files` 白名单加入 `emoji-zh.json`；
  `scripts/verify-manifest.mjs` 把它列为必需发布物。
- `docs/RELEASING.md` 补充 CI 启用的确切步骤与 npm 发布流程。

## [1.2.0] - 2026-09-18

"更好贴、更省事"的一版：匹配放宽到三档并支持近似容错，历史文案也能贴样式，配置从"手调 19 项"变成"点一下"。

### 新增

- **匹配模式三档**（`matchMode`）：`exact` 逐字相同 / `loose` 宽松（Unicode NFKC + 中文标点折叠 + 忽略空白与大小写 + 削首尾标点）/ `fuzzy` 近似容错（编辑距离相似度 ≥ 86%）。
  旧配置里的布尔 `looseMatch` 会自动映射（`false` → `exact`）。
- **只贴助手的回复**（`onlyAssistant`，默认开）：宿主给每个消息块打了 `data-chat-flow-kind`，
  用户消息、工具结果、思考面板一律跳过 —— 你把自己消息里引用的那句开场语粘进对话，也不会再被误贴样式。
- **旧文案兼容表**（`legacyLines`）：历史会话里的旧开场/收尾也能贴样式，可逐条指定用哪一套；只影响显示，**不写进提示词**。
- **动态变量**：`{date}` `{year}` `{month}` `{day}` `{weekday}` `{daypart}` `{time}`。宿主写提示词时就解析，页面按同一规则解析；
  带 `{daypart}` 时页面把所有时段都作为候选，跨时段边界也不会掉样式。
- **多行开场/收尾**：文案里可以换行；宿主把换行渲染成两个段落时，两段都会被贴上同一套样式。
- **深色主题双色**：每行可单独配深色下的文字色 / 底色 / 边框色，留空沿用浅色；宿主用 `body[data-ds-dark-theme]` 标记深色，切换即时生效。
- **样式预设包**：财神金 / 极简灰 / 大字醒目 / 霓虹赛博 / 樱花 / 复古打字，一键套用（只改外观，文案与图片不动；可只套当前行或两行都套）。
- **配置导出 / 导入**：设置页底部按钮，导出 `greet-signoff.json`；导入时先按客户端规则归一化再写回。
- **进度条形态三选**：完整 / 紧凑细条（不显示百分比气泡）/ 只显示一行文字。
- **进度条悬停显示"还能聊多久"**：在占用明细后面追"剩余 ~xx k · 约还能聊 N 轮（最近 N 轮均值 ~xx/轮）"；
  用占用增量估算每轮成本，遇到上下文压缩导致占用回落时自动重置基线。
- **诊断面板**：插件版本、配置文件路径、贴样式器运行次数与上次耗时、上次扫描命中行数与块数、跳过多少非助手块、当前匹配模式、整页命中行数。

### 修复

- **一条回复被工具调用拆成多个块时样式会串**：以前是"块内第一条=开场、最后一条=收尾"，
  于是只有收尾行的那个块会被贴上**开场**样式。现在按"文案身份"分配（命中开场文案就用开场样式），
  只有两行文案其实是同一句时才退回按位置区分。
- **"逐字相同"档仍然会折叠标点**：折叠判断没有走档位，导致选了逐字相同、结尾多一个句号照样命中；现在只有宽松/近似档才折叠。
- **近似档被"块级预筛"挡住**：整句只差一两个字时，原文里既搜不到整句、也搜不到折叠后的整句，
  于是根本没进入匹配环节；现在近似档只拿前 6 个字做探针。
- **旧文案表里的空行**：会拦住保存（否则一保存就被归一化丢掉，看起来像"刚加的行没了"）。

### 变更

- 配置新增 `matchMode` / `onlyAssistant` / `legacyLines`；行级新增 `colorDark` / `bgColorDark` / `borderColorDark`；旧字段 `looseMatch` 仍可读。
- README 补充匹配规则、动态变量、样式预设、导出导入、诊断等说明。

## [1.1.0] - 2026-09-18

发布前的一轮加固：修掉一个"面板里能调、代码里没用"的死配置，补齐公开仓该有的工程件，
并让对话里的固定行在模型写得不那么规整时也能贴上样式。

### 修复

- **删掉死配置 `pollMs`**：设置页原本有「刷新 2000ms」滑条，但代码里从来没有任何轮询用它
  （占用百分比走宿主 `contextPressure` 投影，本来就是实时的）。旧配置里残留的 `pollMs` 会在下次保存时自动丢弃。
- **配置改为原子写**：保存时先写 `greet-signoff.json.tmp` 再改名，并保留上一份 `greet-signoff.json.bak`。
  此前是直接覆盖，若正好在写盘时崩溃/掉电会留下半个 JSON，而读取失败只会静默回落到默认值——表现为"配置莫名丢失"。
- **进度条空状态不再显示 0%**：还没有任何请求记录时显示 `—`，悬停提示「上下文占用未知（发一条消息后显示）」，
  不再让人以为进度条坏了。
- **进度条补无障碍属性**：`role="progressbar"` + `aria-valuenow` / `aria-valuetext`。

### 新增

- **宽松匹配（新配置项 `looseMatch`，默认开）**：正文里的固定行首尾多出引号、括号、星号、句读，
  或大小写不同，也照样贴上样式。可在「设置 → 开场收尾 → 上下文提醒」里切回「逐字相同」。
- **固定行候选标签扩到标题与行内代码**（`h1`–`h6`、`code`）：模型偶尔把固定行写成 `# 标题` 或用反引号包起来，
  以前这类行永远匹配不上；现在会命中，并且同一行同时命中外层与内层时只保留最外层，避免"开场样式被收尾样式顶掉"。
- **到阈值时多一个「开新会话」按钮**：占用超过黄色/红色阈值时，提醒文字后面直接给一个按钮，
  点一下就走客户端 `uiWorkspace.startSession()` 开新会话（拿不到该服务时只显示文字提醒，不显示按钮）。
- **多标签页同步**：切回本标签页 / 重新聚焦窗口时补读一次配置（相比常驻轮询更省电）。
- **`prefers-reduced-motion` 支持**：系统开启"减少动态效果"时，对话里的固定行动效与进度条的告警呼吸、小车浮动一起停掉。
- `CHANGELOG.md`、`scripts/verify-manifest.mjs`（发布自检，`npm run verify`）、`docs/RELEASING.md`（发版流程），
  以及可直接启用的 CI 工作流模板 `docs/ci-workflow.yml`。

### 变更

- 兼容性矩阵补上实测版本 `0.1.5-rc.1`（连同原有 `0.1.2-rc.1`），README 徽章同步。
- `files` 白名单补 `CHANGELOG.md` 与 `docs/`，npm 包里的 README 图片不再失效。
- README 补「与相邻方案的区别」（和 `dsh-context` 的分工、与 `dsh-gold-capsule` 的互斥、接口只监听本机）。

### 性能

- **对话扫样式改为增量**：每个消息块缓存"内容签名 + 匹配结果"，只有真正变了的块才重新扫；
  流式输出时不再每次全量重扫整个会话（此前一次最多 400 块 × 每块 1500 个节点）。
  扫描顺序改为从最新往旧，长会话里屏幕上看得见的消息优先。
- **去掉进度条每 1.5 秒的定位轮询**：改为只靠 `ResizeObserver` + `window resize` + 挂载后两次补测，
  不再在会话期间反复触发强制同步布局（`querySelectorAll` + `getBoundingClientRect`）。
- **宿主提示段加配置缓存**：按 `mtime + 文件大小` 判断是否需要重新读盘，每轮组装提示不再同步读整个配置文件。

## [1.0.0] - 2026-09-17

首个发布版本。

- 每次回复的正文第一行/最后一行渲染成你配置的开场语与收尾语，文字与样式（字号、字重、颜色、动效、形状、
  填充、边框、阴影、字距、大小写、斜体、自定义图片）都可在设置页改，保存后下一次回复即生效。
- 输入框上方一条上下文占用导航条：小车 + 实时百分比，颜色按黄色/红色阈值连续变化，接近上限时整条告警并闪动。
- 宿主半注册系统提示段与 `GET/POST /api/greet-signoff` 配置接口；浏览器半注册设置页、通用页入口与输入区导航条。
